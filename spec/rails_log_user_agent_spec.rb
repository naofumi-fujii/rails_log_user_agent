require "spec_helper"

describe RailsLogUserAgent do
  it "has a version number" do
    expect(RailsLogUserAgent::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
